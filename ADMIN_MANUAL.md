# Streamflow Agency — Полный мини-мануал для администратора

## 1. Что это за проект
Проект состоит из сайта, Telegram-бота и админ-меню в Telegram-группе.

Цель проекта:
- получать анкеты кандидатов из сайта и из бота;
- централизованно обрабатывать заявки в админ-группе;
- публиковать контент в языковые каналы RU/EN/PT/ES из одного источника;
- управлять уже выложенными постами (редактировать текст/медиа, удалять).

## 2. Состав системы

### 2.1. Сайт
Файлы: `web/*.html`, `web/app.js`, `web/styles.css`

Функции сайта:
- мультиязычный интерфейс: RU, EN, PT, ES;
- презентационные страницы проекта;
- многошаговая форма заявки;
- валидация данных до отправки;
- отправка формы в API: `POST /api/apply`;
- автоподстановка ссылок (канал/бот) через `GET /api/config`;
- подключенная Яндекс.Метрика на всех страницах.

### 2.2. Веб-сервис
Файл: `web_server.py`

Функции:
- раздаёт сайт;
- принимает заявки с формы;
- валидирует, нормализует и сохраняет данные;
- отправляет данные в админ-группу;
- обновляет админ-меню (счётчики и уведомления);
- работает с общей БД проекта.

### 2.3. Telegram-бот
Файл: `bot.py`

Функции:
- ведёт пользователей по анкете в Telegram;
- принимает заявки из приватных каналов через join request;
- хранит статусы заявок;
- показывает админ-меню и обрабатывает все админ-действия;
- делает кросспостинг по языкам;
- умеет переводить посты для EN/PT/ES;
- ведёт раздел "Выложенные посты" с редактированием.

### 2.4. База данных
Файл: `database.py`

Хранит:
- заявки (`applications`);
- служебные настройки (`settings`);
- опубликованные посты и их идентификаторы в каналах (`posted_messages`).

Поддержка:
- Postgres (основной);
- SQLite (fallback).

### 2.5. Excel-отчёты
Файл: `excel_export.py`

Функции:
- формирует `applications.xlsx`;
- обновляет статусы в файле;
- пересобирает Excel полностью из БД.

## 3. Логика работы по заявкам

### 3.1. Заявка с сайта
1. Кандидат заполняет форму на сайте.
2. `web_server.py` принимает форму и проверяет данные.
3. Данные сохраняются в БД со статусом `pending` и источником `site`.
4. В админ-группу отправляется информация по заявке.
5. В админ-меню обновляются счётчики и уведомление о новой анкете.

### 3.2. Заявка через бота
1. Пользователь нажимает `/start`.
2. Выбирает язык и проходит анкету в FSM-режиме.
3. Перед отправкой видит предпросмотр и может редактировать поля/фото.
4. После подтверждения данные сохраняются в БД.
5. Админ получает карточку заявки в группе.

### 3.3. Статусы заявок
Основные статусы:
- `new`
- `pending`
- `accepted`
- `rejected`

Админ меняет статус кнопками в карточке заявки.

## 4. Языки и каналы

Поддерживаемые языки:
- RU
- EN
- PT
- ES

Каналы кросспостинга:
- RU: `CHANNEL_ID`
- EN: `CHANNEL_EN_ID`
- PT: `CHANNEL_PT_ID`
- ES: `CHANNEL_ES_ID`

Если EN/PT/ES не настроены, бот предупредит в режиме создания поста.

## 5. Админ-меню: полный список функций

Главное меню содержит:
1. `📝 Создать пост`
2. `📣 Выложенные посты`
3. `⏳ Ожидают подтверждения`
4. `✅ Принятые`
5. `❌ Отклонённые`
6. `📚 Все заявки`
7. `📊 Статистика`
8. `📁 Excel`
9. `🧹 Архивировать старые`
10. `⚠️ Сбросить базу`
11. `🔄 Обновить меню`

## 6. Команды бота

Команды для всех пользователей:
1. `/start`
2. `/language`

Команды для админов в админ-группе:
1. `/admin`
2. `/create_post`
3. `/crosspost`
4. `/stats`
5. `/excel`
6. `/reset_db`

Команды автоматически регистрируются в Telegram-меню команд при старте бота.

## 7. Публикация постов (кросспостинг)

### 7.1. Как публиковать
1. В админ-группе нажать `Создать пост` или отправить `/create_post`.
2. Отправить один пост на русском:
- текст;
- фото + подпись;
- видео + подпись;
- документ + подпись;
- GIF + подпись.
3. Бот автоматически:
- копирует RU-версию в RU-канал;
- переводит текст в EN/PT/ES;
- публикует по соответствующим каналам.

### 7.2. Что сохраняется при переводе и публикации
- форматирование Telegram;
- встроенные гиперссылки (`text_link`);
- URL/mention/hashtag;
- custom/premium emoji (с учётом ограничений Telegram-клиента).

### 7.3. После публикации
Бот сохраняет запись в `posted_messages`:
- тип поста;
- ID сообщений по каналам;
- тексты по языкам;
- entities для корректного последующего редактирования.

## 8. Раздел "Выложенные посты"

Назначение: централизованное управление уже опубликованным контентом.

Для каждого поста доступно:
1. Просмотр карточки поста.
2. Просмотр полного RU-текста.
3. Медиа-превью поста в админ-группе (если пост медийный).
4. `Изменить текст`:
- отправляете новый RU-текст;
- бот делает переводы;
- обновляет текст во всех каналах.
5. `Изменить медиа`:
- тип зависит от поста (фото/видео/файл/GIF);
- бот заменяет медиа во всех каналах;
- подписи по языкам сохраняются.
6. `Удалить из каналов`:
- пост удаляется из всех каналов по сохранённым message_id.
7. Переход по постам: предыдущий/следующий.

## 9. Работа с заявками в админке

Основной сценарий:
1. Открыть раздел `Ожидают подтверждения`.
2. Проверить карточку и фото кандидата.
3. Принять или отклонить.
4. При отклонении выбрать шаблон или указать свою причину.
5. Причина отправляется на языке подачи заявки.

Дополнительно:
- `Все заявки` — общий архив карточек;
- `Статистика` — оперативные числа по статусам;
- `Excel` — выгрузка базы заявок в файл.

## 10. Автоматические фоновые процессы

В проекте работают периодические задачи:
1. Ежедневная отправка статистики в админ-группу.
2. Периодическая архивация старых админ-сообщений.
3. Очистка временных служебных сообщений в админке.

## 11. Переменные окружения (обязательно)

Общие:
- `SITE_URL`
- `DATABASE_URL`
- `APP_TIMEZONE`

Для бота:
- `BOT_TOKEN`
- `ADMIN_GROUP_ID`
- `ADMIN_USERNAME`
- `CHANNEL_ID`
- `CHANNEL_EN_ID`
- `CHANNEL_PT_ID`
- `CHANNEL_ES_ID`
- `OPENAI_API_KEY`
- `OPENAI_TRANSLATE_MODEL`
- `OPENAI_HTTP_TIMEOUT_SECONDS`

Для веб-сервиса:
- `BOT_TOKEN`
- `ADMIN_GROUP_ID`
- `ADMIN_USERNAME`
- `BOT_USERNAME`
- `CHANNEL_LINK`
- `HOST`
- `WEB_SEND_FULL_TO_ADMIN`

Важно:
- bot и web должны использовать один и тот же `DATABASE_URL`.

## 12. Ежедневный регламент администратора

1. Открыть `/admin`.
2. Проверить `Ожидают подтверждения`.
3. Обработать новые заявки.
4. Проверить `Статистику`.
5. При необходимости выгрузить `Excel`.
6. При контент-плане: через `Создать пост` выложить пост в каналы.
7. При правках контента: раздел `Выложенные посты`.

## 13. Краткая карта файлов проекта

- `bot.py` — логика Telegram-бота и админки
- `web_server.py` — API формы и веб-сервер
- `database.py` — БД и функции хранения
- `keyboards.py` — inline-клавиатуры
- `states.py` — FSM-состояния
- `texts.py` — мультиязычные тексты
- `excel_export.py` — Excel-выгрузка
- `config.py` — чтение env и конфиг
- `web/*` — фронтенд сайта

